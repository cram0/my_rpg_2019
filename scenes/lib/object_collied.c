#include "object_collied.h"

void collied_objects(object *objects, animation *ani, int attack)
{
    // sfFloatRect lnk = sfSprite_getGlobalBounds(ani->sprite);
    // sfFloatRect obj = sfSprite_getGlobalBounds(objects->ani.sprite);

    // if (attack != 1) return;
    // if (sfFloatRect_intersects(&lnk, &obj, NULL)) {
    //     objects->not_draw = 1;
    // }
}